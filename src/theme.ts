import { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
  colors: {
    primaryText: "#000000",
    secondaryText: "#2653FF",
    background: "#ffffff"
  }
};

export { theme };
